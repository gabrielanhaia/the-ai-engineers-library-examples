# Copy to .env and fill in only what the lab you are running needs.
# Never commit .env. CI fails the build if one is committed.

# Only needed for the optional labs that call a hosted API for comparison.
OPENAI_API_KEY=
